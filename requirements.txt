requests>=2.31
beautifulsoup4>=4.12
# Optional, only needed for --render:
# playwright>=1.40
# Optional, only needed for --chart:
# matplotlib>=3.6
# Optional, only needed for the web UI (logogrowth-web):
# flask>=2.3
